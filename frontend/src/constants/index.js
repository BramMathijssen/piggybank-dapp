const tokenCreatorAbi = require("./tokenCreator.abi.json");
const tokenCreatorBytecode = require("./tokenCreator.byteCode.json");
const contractAddresses = require("./contractAddresses.json")
const parentContractAbi = require("./parentContract.abi.json")

module.exports = {
    tokenCreatorAbi,
    tokenCreatorBytecode,
    contractAddresses,
    parentContractAbi
};
